spring.application.name=odoru

# MongoDB (utilise MONGODB_URI sur Render, localhost par defaut en local)
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/odoru}

# Server (port dynamique pour Render, 8080 en local)
server.port=${PORT:8080}
